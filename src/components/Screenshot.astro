---
import { Image } from "astro:assets";
const { alt, src, width = 1920, height = 1080 } = Astro.props;
---

<Image
  src={`https://nadav-website.s3.us-west-001.backblazeb2.com/${src}`}
  alt={alt}
  width={width}
  height={height}
  sizes="100vw, (min-width: 70rem) 50vw"
/>

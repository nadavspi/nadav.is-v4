---
import { formatDistanceToNow } from "date-fns";

interface Props {
  date: Date;
}

const { date } = Astro.props;
const longAgo = formatDistanceToNow(date, { addSuffix: true });
const capitalized = [...longAgo][0] + longAgo.slice(1);
---

Written {capitalized}
